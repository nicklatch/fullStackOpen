import { useDispatch, useSelector } from 'react-redux';
import { captureVote } from '../reducers/anecdoteReducer';

const Anecdote = ({ props }) => {
  return <>anecdotes</>;
};

const AnecdoteList = () => {
  return <li>// TODO: Pickup here!</li>;
};

export default AnecdoteList;
